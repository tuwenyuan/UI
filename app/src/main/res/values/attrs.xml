<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width" format="dimension"/>
        <attr name="civ_border_color" format="color"/>
        <attr name="civ_border_overlay" format="boolean"/>
        <attr name="civ_fill_color" format="color"/>
    </declare-styleable>

    <declare-styleable name="PieView">
        <attr name="centerTextSize" format="dimension" />
        <attr name="dataTextSize" format="dimension" />
        <attr name="centerTextColor" format="color" />
        <attr name="dataTextColor" format="color" />
        <attr name="circleWidth" format="dimension" />
    </declare-styleable>

    <!--SwitchHeaderButton-->
    <declare-styleable name="SwitchHeaderButton">
        <attr name="rightText" format="string"/>
        <attr name="leftText" format="string"/>
    </declare-styleable>

    //圆形头像及文字
    <declare-styleable name="CircleTextImageView">
        <attr name="citv_border_width" format="dimension"/>
        <attr name="citv_border_color" format="color"/>
        <attr name="citv_border_overlay" format="boolean"/>
        <attr name="citv_fill_color" format="color"/>
        <attr name="citv_text_text" format="string"/>
        <attr name="citv_text_color" format="color"/>
        <attr name="citv_text_size" format="dimension"/>
        <attr name="citv_text_padding" format="dimension"/>
    </declare-styleable>

    <declare-styleable name="QRCodeView">
        <!-- 扫描框距离扫描视图顶部的距离，默认值为90dp -->
        <attr name="qrcv_topOffset" format="dimension|reference"/>
        <!-- 扫描框边角线的宽度，默认值为3dp -->
        <attr name="qrcv_cornerSize" format="dimension|reference"/>
        <!-- 扫描框边角线的长度，默认值为20dp -->
        <attr name="qrcv_cornerLength" format="dimension|reference"/>
        <!-- 扫描框边角线的颜色，默认值为@android:color/white -->
        <attr name="qrcv_cornerColor" format="color|reference"/>
        <!-- 扫描框的宽度，默认值为200dp -->
        <attr name="qrcv_rectWidth" format="dimension|reference"/>
        <!-- 除去扫描框，其余部分阴影颜色，默认值为#33FFFFFF -->
        <attr name="qrcv_maskColor" format="color|reference"/>
        <!-- 扫描线的宽度，默认值为1dp -->
        <attr name="qrcv_scanLineSize" format="dimension|reference"/>
        <!-- 扫描线的颜色，默认值为@android:color/white -->
        <attr name="qrcv_scanLineColor" format="color|reference"/>
        <!-- 扫描线距离左右边框的间距，默认值为0dp -->
        <attr name="qrcv_scanLineHorizontalMargin" format="dimension|reference"/>
        <!-- 是否显示默认的图片扫描线，默认值为false -->
        <attr name="qrcv_isShowDefaultScanLineDrawable" format="boolean"/>
        <!-- 扫描线的图片资源，默认值为空 -->
        <attr name="qrcv_customScanLineDrawable" format="reference"/>
        <!-- 扫描边框的宽度，默认值为1dp -->
        <attr name="qrcv_borderSize" format="dimension|reference"/>
        <!-- 扫描边框的颜色，默认值为@android:color/white -->
        <attr name="qrcv_borderColor" format="color|reference"/>
        <!-- 扫描线从顶部移动到底部的动画时间，默认值为1000 -->
        <attr name="qrcv_animTime" format="integer"/>
    </declare-styleable>

    <declare-styleable name="PrecentRelativeLayout">
        <attr name="layout_widthPrecent" format="float"></attr>
        <attr name="layout_heightPrecent" format="float"></attr>
    </declare-styleable>
    <declare-styleable name="PrecentLinearLayout">
        <attr name="adapter_width" format="integer"></attr>
        <attr name="adapter_height" format="integer"></attr>
        <attr name="adapter_margin_top" format="integer"></attr>
        <attr name="adapter_margin_bottom" format="integer"></attr>
        <attr name="adapter_margin_left" format="integer"></attr>
        <attr name="adapter_margin_right" format="integer"></attr>
    </declare-styleable>

    <declare-styleable name="DragScrollDetailsLayout">
        <!-- float value for indicate the moment of switch panel-->
        <attr name="percent" format="float"/>
        <!-- how long the animation keep-->
        <attr name="duration" format="integer"/>
        <!-- default panel to show after init-->
        <attr name="default_panel" format="enum">
            <enum name="front" value="0"/>
            <enum name="behind" value="1"/>
        </attr>
    </declare-styleable>

</resources>
